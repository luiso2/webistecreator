{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile",
    "watchPatterns": ["/railway-forge/**"]
  },
  "deploy": { "restartPolicyType": "ALWAYS", "numReplicas": 4 }
}
